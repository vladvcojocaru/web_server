document.addEventListener('DOMContentLoaded', () => {
    console.log('JavaScript is loaded!');
    const message = document.getElementById('message');
    message.textContent = 'Hello, JavaScript has updated this message!';
});
